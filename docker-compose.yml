version: "3.8"
services:
  api:
    container_name: api
    build:
      context: ./docker/server
      dockerfile: Dockerfile
    tty: true
    ports:
      - "3000:3000"
    volumes:
      - type: bind
        source: .
        target: /api